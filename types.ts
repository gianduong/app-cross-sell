
export interface AppInfo {
  id: string;
  name: string;
  description: string;
  logo: string;
  appStoreUrl: string;
  color: string;
  isInstalled: boolean;
}

export interface Point {
  x: number;
  y: number;
}
